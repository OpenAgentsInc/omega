[package]
name = "omega_control_crawl"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
license = "GPL-3.0-or-later"
description = "Hermetic control-crawl protocol: drawn implies working (OMEGA-DELTA-0187)"

[lints]
workspace = true

[lib]
path = "src/omega_control_crawl.rs"
doctest = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
