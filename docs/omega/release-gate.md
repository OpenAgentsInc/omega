# Omega installed-candidate release gate

This report is generated by `script/omega-release-gate` (OMEGA-DELTA-0185) from a packaged candidate produced by `script/bundle-omega-rc` and launched from a clean profile. Row meanings: `automated-pass`/`automated-fail` were observed end-to-end by the harness; `owner-assisted-pending` rows require a human account, interactive provider auth, a second person, or a judgment call, and carry the exact instruction; `blocked` rows name the host condition that refused observation. No row is ever fabricated.

- Generated: 2026-07-30T13:32:37+00:00
- Candidate: `Omega-v0.2.0-rc27-macos-arm64.dmg` (version 0.2.0-rc27, channel rc)
- Package SHA-256: `800a88e6bb588cc9c1238d0dceb59e59c8a700abb6ba2ceca5c28abae96f53d4`
- Source commit: `6a1e11491849d4b1a2327c454b297b82a1d91981`
- Harness commit: `6a1e11491849d4b1a2327c454b297b82a1d91981`
- Overall status: **automated_green_pending_owner**
- Receipt digest: `6900ce120a64c009ad0dead19d515ed40ca683c0a5cfcdea32738911aa2a14c3`

| Row | Status | Issues | Evidence / owner instruction |
| --- | --- | --- | --- |
| `package-integrity` | automated-pass | — | package-integrity.json |
| `flag-free-launch` | automated-pass | omega#151 | flag-free-launch.png; flag-free-launch-ocr.json |
| `version-truth` | automated-pass | — | version-truth.json |
| `zero-refusal-log-sweep` | automated-pass | — | launch-log.log |
| `tester-channel-destination` | automated-pass | omega#156 | flag-free-launch.png; flag-free-launch-ocr.json |
| `update-safety-staged` | automated-pass | — | update-safety.json |
| `menu-honesty` | owner-assisted-pending | — | Open every menu in the running candidate and confirm each item either performs its action or is disabled; no item may dispatch a zero-base refusal (the automated log sweep would have caught a refusal fired during the scripted journey, but only a person can exhaustively pull every menu item). |
| `direct-codex` | owner-assisted-pending | omega#152 | In the staged candidate (launched with a clean --user-data-dir), open the composer executor dropdown, choose Codex, and complete one turn that streams and uses a tool; stop it mid-turn; resume; quit and relaunch; confirm the restored thread still names Codex in title, composer label, and disclosure. Codex auth must use an ISOLATED home (for example CODEX_HOME=$(mktemp -d) codex login --device-auth) — NEVER reuse or copy ~/.codex or any owner credential file into the candidate profile. |
| `direct-claude-code` | owner-assisted-pending | omega#152 | Same journey as the Codex row with Claude Code selected. Use an isolated CLAUDE_CONFIG_DIR for auth; never reuse or copy the owner's ~/.claude state. |
| `direct-grok` | owner-assisted-pending | omega#152 | Same journey with Grok Build selected: the native ACP server must launch with the shipped command order. Use an isolated Grok config/auth home; never reuse owner credentials. |
| `generic-acp` | owner-assisted-pending | omega#152 | Use Add More Agents to open the ACP registry visibly, add one agent that is not Codex/Claude/Grok, authenticate through that agent's own flow if required, run one turn, relaunch, and confirm the restored session names the same agent. |
| `concurrency-supervision` | owner-assisted-pending | omega#155 | Run two different direct agents concurrently; switch views between them; cancel one and confirm the other continues; point a second write-capable agent at the occupied worktree and confirm the collision rule fires visibly; relaunch and confirm both threads restore with honest Failed/Cancelled lifecycle states and independent history. |
| `three-mode-journeys` | owner-assisted-pending | omega#151 | From a clean profile, create one conversation in each mode (Direct Agent, Omega Agent, Sarah) twice: once keyboard-only, once pointer-only, all through the composer executor dropdown. Confirm transcript title, composer label, and disclosure agree on the executor each time, and every dropdown row shows one of the four honest readiness states. |
| `omega-routing` | owner-assisted-pending | — | Send two materially different Omega Agent fixtures; confirm they choose different eligible executors and that decision, reason, override, fallback, and disclosure are visible and durable. |
| `sarah-journey` | owner-assisted-pending | — | As an eligible alpha user: confirm price/hold/limit renders before the microphone opens, start voice, confirm one bounded command, start an agent thread, survive a transient reconnect, end, and confirm the final charge. |
| `channels-send-receive` | owner-assisted-pending | omega#156 | From the staged candidate send one message to the tester destination; confirm a SECOND account receives it there; kill the relay connection and confirm the outage state renders the documented fallback, not a spinner; exercise moderation/reporting once. |
| `work-surfaces` | owner-assisted-pending | — | Walk every visible Files/Search/Git/Terminal/Review/Settings/agent-registry/Sarah action in the installed candidate; each must work or be visibly disabled with a reason. The hermetic workbench proof (script/omega-workbench-proof) covers the rendered states and is attached as supporting evidence, not as the installed verdict. |
| `update-safety-lifecycle` | owner-assisted-pending | — | Drag the DMG's Omega.app beside the existing Zed and OpenAgents Desktop installs, launch it, then upgrade, roll back, and uninstall; confirm only Omega paths change (the automated row already proves a staged run leaves neighbouring app data untouched). |
| `distribution` | owner-assisted-pending | — | When the public download page exists, confirm page identity, version, platform, artifact digest, signature/notarization state, release notes, and the installed binary agree with this receipt's candidate binding. |
| `independent-review` | owner-assisted-pending | — | Have a reviewer who did not produce the candidate repeat the held-out journey and record the verdict against this receipt's candidate digest. |

